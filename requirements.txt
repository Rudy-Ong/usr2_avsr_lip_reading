torch>=2.0
torchaudio>=2.0
torchvision>=0.15
pytorch-lightning>=2.0
hydra-core>=1.3
omegaconf>=2.3
opencv-python
torchmetrics
numpy
tqdm
av
mediapipe
# Optional: ibug backend (higher accuracy, requires manual install, see setup_ibug.sh)
# ibug.face_detection and ibug.face_alignment must be installed from source
